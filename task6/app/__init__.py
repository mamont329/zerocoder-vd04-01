"""Инициализация пакета-приложения.

Здесь создаётся сам объект Flask (app). А маршруты лежат в отдельном
файле routes.py — импортируем его В КОНЦЕ, когда app уже создан,
иначе routes.py не сможет на него сослаться.
"""
from flask import Flask

app = Flask(__name__)

from app import routes  # noqa: E402  (импорт в конце — так и задумано)
