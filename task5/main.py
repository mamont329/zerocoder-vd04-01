"""Точка запуска приложения.

ВАЖНО: перед ПЕРВЫМ запуском один раз создай базу данных:
    python create_db.py        # создаст instance/site.db с таблицей users
Иначе при регистрации/входе будет ошибка "no such table: user".

База — это файл на диске, она сохраняется между запусками. Поэтому
create_db.py нужен только один раз (или если удалил базу / сменил модель).

Обычный запуск (когда база уже создана):
    python main.py
"""
from app import app

if __name__ == "__main__":
    app.run(debug=True)
